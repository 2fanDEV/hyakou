# Cargo.toml for the frontend package, solely due to cargo-watch so that we have
# the possibility to watch for changes in the rust backendc
[package]
name = "frontend"
version = "0.1.0"
edition = "2024"